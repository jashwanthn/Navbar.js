2) Create / Replace src/overrides.css (this forces safe layout with !important)
/* overrides.css
   A small set of strong rules to ensure the content never sits under the header/sidebar.
   Adjust the values below if your header or sidebar sizes are different.
*/

/* Combined header heights used across our examples:
   top bar = 34px, navbar = 70px -> total header = 104px
   sidebar width = 240px
*/
.content-with-sidebar {
  margin-left: 240px !important; /* push content right of sidebar */
  box-sizing: border-box !important;
  width: calc(100% - 240px) !important;
}

/* site-body-padding pushes content below the fixed header */
.site-body-padding {
  padding-top: 104px !important; /* push content below topbar+navbar */
  box-sizing: border-box !important;
}

/* If other CSS added padding/margin to main, normalize it */
.content-with-sidebar,
.site-body-padding {
  margin: 0 !important;
}

/* Ensure the AccessConfiguration card won't overlap left edge by force-centering it
   inside the content column and restricting max width. */
.ac-wrapper {
  padding-left: 24px !important;
  padding-right: 24px !important;
  padding-top: 24px !important;
  min-height: calc(100vh - 104px) !important;
}

/* card restrictions */
.ac-card {
  width: 100% !important;
  max-width: 1100px !important;
  margin: 0 auto !important;
  box-sizing: border-box !important;
}

/* if something else sets position/transform on the card, reset it */
.ac-card,
.ac-wrapper,
.content-with-sidebar {
  position: relative !important;
  transform: none !important;
}

/* If your page had extra global padding causing the white gap at edges, zero it */
html, body, #root {
  margin: 0 !important;
  padding: 0 !important;
  height: 100% !important;
}

/* small safeguard: if a higher-specificity rule made navbar absolute with overlap,
  this makes sure the content wrapper is visible and not hidden by z-index issues */
.content-with-sidebar {
  z-index: 1 !important;
}

/* Table wrapper should scroll inside the card, not overflow the page */
.ac-table-wrap {
  overflow-x: auto !important;
}

/* If you want the Add button to match header gold */
.ac-btn-primary {
  background: #ffb400 !important;
  color: #003366 !important;
  border: none !important;
}

3) Replace src/components/AccessConfiguration.css with this (ensures no overflow & fits)
/* AccessConfiguration.css - strict layout that works with overrides.css */

/* wrapper sits inside the content column */
.ac-wrapper {
  box-sizing: border-box;
  padding: 24px;
  min-height: calc(100vh - 104px);
  width: 100%;
  background: transparent;
}

/* card constrained to available content area (centered) */
.ac-card {
  background: #ffffff;
  max-width: 1100px;
  width: 100%;
  margin: 0 auto;
  border-radius: 10px;
  border: 1px solid rgba(0,0,0,0.06);
  box-shadow: 0 6px 18px rgba(0,0,0,0.06);
  overflow: hidden;
  box-sizing: border-box;
}

/* header row of card */
.ac-header {
  display: grid;
  grid-template-columns: 1fr min-content;
  align-items: center;
  gap: 12px;
  padding: 18px 20px;
  border-bottom: 1px solid rgba(0,0,0,0.06);
  background: #fff;
}

.ac-title { font-size: 20px; font-weight: 600; color: #0f172a; }

/* Controls */
.ac-controls { padding: 18px 20px; border-bottom: 1px solid rgba(0,0,0,0.04); }
.ac-row { display: grid; grid-template-columns: 140px 1fr 140px 220px 120px; gap: 12px; align-items: center; }
.ac-input, .ac-select { padding: 10px 12px; border: 1px solid rgba(15,23,42,0.08); border-radius: 8px; width: 100%; box-sizing: border-box; }

/* make Add button gold to match header */
.ac-add-col { text-align: right; }
.ac-btn-primary { display:inline-block; background:#ffb400; color:#003366; padding:10px 16px; border-radius:8px; font-weight:600; border:none; cursor:pointer; }

/* table area */
.ac-table-wrap { padding: 18px 20px 26px 20px; overflow-x: auto; }
.ac-table { width: 100%; border-collapse: collapse; font-size: 14px; }
.ac-table thead th { text-align: left; padding: 10px 12px; background: #f8fafc; border-bottom: 1px solid rgba(0,0,0,0.06); font-weight: 600; }
.ac-table tbody td { padding: 12px; border-bottom: 1px solid rgba(0,0,0,0.04); color: #374151; vertical-align: top; }
.ac-empty { text-align: center; padding: 30px 12px; color: #6b7280; }

/* small screen adjustments: keep card inside content area */
@media (max-width: 1100px) {
  .ac-card { margin-left: 12px; margin-right: 12px; }
  .ac-row { grid-template-columns: 120px 1fr; grid-auto-rows: auto; }
  .ac-add-col { grid-column: 1 / -1; text-align: left; margin-top: 8px; }
}
